---
phase: 08-jukebox
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - ~/decibel-mobile/app/jukebox.tsx
  - ~/decibel-mobile/app/(tabs)/index.tsx
  - ~/decibel-mobile/src/components/jukebox/JukeboxCard.tsx
  - ~/decibel-mobile/src/components/jukebox/EmbeddedPlayer.tsx
autonomous: true
requirements: [JBX-01, JBX-04, JBX-05, JBX-06, JBX-07, JBX-08, JBX-09, JBX-11]

must_haves:
  truths:
    - "Map icon on Home is replaced with Jukebox (ListMusic) icon that navigates to /jukebox"
    - "Jukebox screen shows a FlatList of JukeboxCards with finder info, artist, and embedded player"
    - "Max 3 WebViews are active at any time; scrolling evicts oldest"
    - "WebView audio is paused via injectJavaScript before unmounting"
    - "mediaPlaybackRequiresUserAction is true — opening Jukebox does not interrupt background music"
    - "Empty state shows when no Finds are available"
    - "Discover button on each card calls useDiscoverArtist mutation with haptic feedback"
  artifacts:
    - path: "~/decibel-mobile/app/jukebox.tsx"
      provides: "Jukebox screen with FlatList, WebView pool, empty state, discover wiring"
      min_lines: 80
    - path: "~/decibel-mobile/src/components/jukebox/JukeboxCard.tsx"
      provides: "Card with finder avatar+name, artist name+photo, platform badge, Discover button slot"
      min_lines: 60
    - path: "~/decibel-mobile/src/components/jukebox/EmbeddedPlayer.tsx"
      provides: "WebView wrapper with isActive prop for pool management"
      min_lines: 40
  key_links:
    - from: "~/decibel-mobile/app/(tabs)/index.tsx"
      to: "/jukebox"
      via: "router.push on ListMusic icon press"
      pattern: "router\\.push.*jukebox"
    - from: "~/decibel-mobile/app/jukebox.tsx"
      to: "useJukebox"
      via: "hook call for feed data"
      pattern: "useJukebox"
    - from: "~/decibel-mobile/app/jukebox.tsx"
      to: "useDiscoverArtist"
      via: "hook call for discover mutation passed to JukeboxCard onDiscover"
      pattern: "useDiscoverArtist"
    - from: "~/decibel-mobile/src/components/jukebox/EmbeddedPlayer.tsx"
      to: "react-native-webview"
      via: "WebView component with mediaPlaybackRequiresUserAction"
      pattern: "mediaPlaybackRequiresUserAction"
---

<objective>
Build the Jukebox UI: swap the Home map icon for Jukebox navigation, create the Jukebox screen with FlatList + WebView pool, JukeboxCard component, and EmbeddedPlayer with max-3 active WebViews. Wire up the Discover button via useDiscoverArtist.

Purpose: This is the visual and interactive layer of the Jukebox feature — users can browse and listen to music from their social feed, and discover artists with one tap.
Output: Working Jukebox screen accessible from Home, with embedded players, proper WebView lifecycle management, and functional Discover button.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-jukebox/08-RESEARCH.md
@.planning/phases/08-jukebox/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From ~/decibel-mobile/src/types/jukebox.ts:
```typescript
export type JukeboxItem = {
  id: string;
  fan_id: string;
  fan_name: string;
  fan_avatar: string | null;
  created_at: string;
  performer_id: string;
  performer_name: string;
  performer_slug: string;
  performer_photo: string | null;
  genres: string[] | null;
  platform: 'spotify' | 'soundcloud' | 'apple_music' | null;
  embed_url: string | null;
  spotify_url: string | null;
  soundcloud_url: string | null;
  apple_music_url: string | null;
};

export type JukeboxResponse = {
  items: JukeboxItem[];
  hasNextPage: boolean;
  isFallback: boolean;
};
```

From ~/decibel-mobile/src/hooks/useJukebox.ts:
```typescript
export function useJukebox(): UseInfiniteQueryResult<JukeboxResponse>;
```

From ~/decibel-mobile/src/hooks/useMyCollectedIds.ts:
```typescript
export function useMyCollectedIds(): { collectedIds: Set<string>; isLoading: boolean };
```

From ~/decibel-mobile/src/hooks/useDiscoverArtist.ts:
```typescript
// Existing hook — returns a mutation that calls POST /api/mobile/discover
export function useDiscoverArtist(): UseMutationResult;
// Usage: const { mutate: discoverArtist } = useDiscoverArtist();
//        discoverArtist({ performerId: "..." });
```

From ~/decibel-mobile/app/(tabs)/index.tsx (line ~84-97):
```typescript
// Current Map icon — REPLACE this:
<Pressable onPress={() => Alert.alert("Coming Soon", ...)}>
  <Map size={20} color={colors.textSecondary} />
</Pressable>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmbeddedPlayer + JukeboxCard components</name>
  <files>
    ~/decibel-mobile/src/components/jukebox/EmbeddedPlayer.tsx
    ~/decibel-mobile/src/components/jukebox/JukeboxCard.tsx
  </files>
  <action>
1. **EmbeddedPlayer** at `src/components/jukebox/EmbeddedPlayer.tsx`:
   - Props: `{ embedUrl: string; isActive: boolean; height?: number }`
   - When `isActive = false`: render a placeholder View with same dimensions (background color from theme, rounded corners, centered music icon)
   - When `isActive = true`: render `<WebView>` with:
     - `source={{ uri: embedUrl }}`
     - `mediaPlaybackRequiresUserAction={true}` (JBX-07 — CRITICAL: prevents background music interruption)
     - `allowsInlineMediaPlayback={true}`
     - `javaScriptEnabled={true}`
     - `scrollEnabled={false}`
     - `style={{ height: height ?? 152, borderRadius: 12 }}`
   - Store ref: `const webViewRef = useRef<WebView>(null)`
   - useEffect cleanup when `isActive` changes to false: call `webViewRef.current?.injectJavaScript("document.querySelectorAll('audio,video').forEach(el => el.pause()); true;")` (JBX-08)
   - Default height: 152 for Spotify/Apple Music, 166 for SoundCloud (visual param from embed formats)

2. **JukeboxCard** at `src/components/jukebox/JukeboxCard.tsx`:
   - Props: `{ item: JukeboxItem; isPlayerActive: boolean; isCollected: boolean; onDiscover: (performerId: string) => void }`
   - Layout (vertical card):
     - Top row: finder avatar (24px circle, fallback initials) + finder name + time ago (use `formatDistanceToNow` or manual relative time)
     - Artist section: artist photo (48px rounded), artist name (semibold), platform badge icon (Spotify green / SoundCloud orange / Apple Music pink)
     - Embedded player: `<EmbeddedPlayer embedUrl={item.embed_url} isActive={isPlayerActive} />` — only render if `embed_url` is not null; if null, show "No preview available" text
     - Bottom: Discover button — if `isCollected`, show muted "In Passport" text; otherwise show pink "Discover" Pressable that calls `onDiscover(item.performer_id)`
   - Use theme colors from useTheme context (dark/light mode)
   - Card styling: theme card background, 16px padding, 12px border radius, soft shadow
   - Tapping artist name/photo navigates to `/artist/${item.performer_slug}`
  </action>
  <verify>
    <automated>cd ~/decibel-mobile && npx tsc --noEmit src/components/jukebox/EmbeddedPlayer.tsx src/components/jukebox/JukeboxCard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>EmbeddedPlayer renders WebView with mediaPlaybackRequiresUserAction=true and pauses audio on deactivation. JukeboxCard shows finder info, artist, embedded player, and Discover button with collected state.</done>
</task>

<task type="auto">
  <name>Task 2: Jukebox screen + Home icon swap + Discover wiring</name>
  <files>
    ~/decibel-mobile/app/jukebox.tsx
    ~/decibel-mobile/app/(tabs)/index.tsx
  </files>
  <action>
1. **Swap Map icon** in `app/(tabs)/index.tsx`:
   - Replace `import { Search, Map, Compass }` with `import { Search, ListMusic, Compass }` from lucide-react-native
   - Replace `<Map size={20} .../>` with `<ListMusic size={20} color={colors.textSecondary} />`
   - Replace the `onPress` Alert handler with `() => router.push("/jukebox")`
   - Import `useRouter` from expo-router if not already imported

2. **Jukebox screen** at `app/jukebox.tsx`:
   - Full-screen route (not a tab) with back navigation header
   - Header: back arrow + "Jukebox" title + optional subtitle showing "From people you follow" or "From the community" based on `isFallback`
   - Use `useJukebox()` hook for data
   - Use `useMyCollectedIds()` to check if user already collected each artist
   - **Wire useDiscoverArtist (JBX-09):**
     - Import `useDiscoverArtist` from `@/hooks/useDiscoverArtist`
     - Import `* as Haptics` from `expo-haptics`
     - Call the hook: `const { mutate: discoverArtist } = useDiscoverArtist();`
     - Create handler: `const handleDiscover = (performerId: string) => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); discoverArtist({ performerId }); }`
     - Pass to each JukeboxCard: `<JukeboxCard onDiscover={handleDiscover} ... />`
   - FlatList rendering JukeboxCards:
     - `keyExtractor`: `item.id`
     - `ItemSeparatorComponent`: 16px spacer
     - `contentContainerStyle`: padding 16px horizontal, bottom padding for tab bar (100px)
   - **WebView pool (JBX-06):**
     - State: `const [activeKeys, setActiveKeys] = useState<Set<string>>(new Set())`
     - `viewabilityConfig` ref: `{ itemVisiblePercentThreshold: 50, minimumViewTime: 300 }`
     - `onViewableItemsChanged` callback: collect visible item keys, keep max 3 in Set (evict oldest when > 3), call `setActiveKeys`
     - Pass `isPlayerActive={activeKeys.has(item.id)}` to each JukeboxCard
   - **Empty state (JBX-11):** When `useJukebox` returns empty data, show centered empty state: music note icon + "No Finds yet" title + "Follow people to see their discoveries here" subtitle
   - **Infinite scroll:** `onEndReached` calls `fetchNextPage`, `onEndReachedThreshold={0.5}`
   - Loading state: ActivityIndicator while `isLoading`
   - Pull-to-refresh: `refreshControl` calling `refetch()`
   - Use theme colors throughout (dark/light mode compatible)
  </action>
  <verify>
    <automated>cd ~/decibel-mobile && npx tsc --noEmit app/jukebox.tsx app/\(tabs\)/index.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Home screen shows ListMusic icon that navigates to /jukebox. Jukebox screen renders FlatList with max-3 WebView pool, empty state, infinite scroll, and pull-to-refresh. Discover button calls useDiscoverArtist with haptic feedback. Both dark and light mode work.</done>
</task>

</tasks>

<verification>
- Home screen map icon replaced with ListMusic icon
- Tapping ListMusic navigates to /jukebox screen
- Jukebox FlatList renders JukeboxCards with embedded players
- Scrolling past 3 cards deactivates oldest WebView (placeholder shown)
- Discover button triggers useDiscoverArtist mutation with haptic feedback
- Empty state appears when no data
- TypeScript compiles with no errors
</verification>

<success_criteria>
- JBX-01: Map button replaced with Jukebox icon, navigation works
- JBX-04: Cards show finder avatar+name+timeago, artist name+platform badge
- JBX-05: Embedded players render via WebView for each platform
- JBX-06: Max 3 active WebViews enforced by pool
- JBX-07: mediaPlaybackRequiresUserAction=true on all WebViews
- JBX-08: Audio paused via injectJavaScript before WebView unmount
- JBX-09: Discover button calls useDiscoverArtist with haptic feedback, card state updates
- JBX-11: Empty state displayed when no Finds
</success_criteria>

<output>
After completion, create `.planning/phases/08-jukebox/08-02-SUMMARY.md`
</output>
