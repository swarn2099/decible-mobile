# Phase 7 — Post-Build Bugs

**Reported:** 2026-03-12
**Status:** Open

1. **Remove card tilt/rotation** — Cards are tilted, Swarn doesn't like it. Remove all rotation from GlassCards.

2. **Fix 2x4 grid layout** — Cards are rendering in a single column instead of a 2-column grid. Fix GlassGrid to show 2 columns x 4 rows.

3. **ScrollView broken** — Can't scroll down to badges section below the PagerView tabs. The passport screen scroll is not working.

4. **Other user profiles need glassmorphic design** — When viewing another user's profile, it should match the new glassmorphic passport design (orbs, glass cards, tabs).

5. **Settings and leaderboard icons overlap** — The settings gear and trophy icons are overlapping each other in the header area.
