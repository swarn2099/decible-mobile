---
phase: 07-glassy-passport-redesign
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/passport/OrbBackground.tsx
  - src/components/passport/GlassCard/StampGlassCard.tsx
  - src/components/passport/GlassCard/FindGlassCard.tsx
  - src/components/passport/GlassCard/DiscoveryGlassCard.tsx
  - src/components/passport/GlassGrid.tsx
autonomous: true
requirements:
  - GPASS-03
  - GPASS-04
  - GPASS-05
  - GPASS-06
  - GPASS-07
  - GPASS-08
  - GPASS-11
  - GPASS-12
  - GPASS-13

must_haves:
  truths:
    - "Animated gradient orbs drift slowly behind content, shifting color emphasis based on active tab index"
    - "Stamp glass cards show full-bleed artist photo with pink-tinted glass strip containing artist name, venue, date, and Founder badge"
    - "Find glass cards show full-bleed artist photo with purple-tinted glass strip containing artist name, platform icon, listener count, and Founder badge"
    - "Discovery glass cards show full-bleed artist photo with blue-tinted glass strip containing artist name and 'via @finder' tappable link"
    - "All cards have slight rotation (-2 to +2 degrees), press-in scale animation (0.97), and light haptic on tap"
    - "GlassGrid renders 2x4 cards without empty placeholders when fewer than 8 items exist"
  artifacts:
    - path: "src/components/passport/OrbBackground.tsx"
      provides: "3 animated gradient orbs with tab-reactive opacity"
      exports: ["OrbBackground"]
    - path: "src/components/passport/GlassCard/StampGlassCard.tsx"
      provides: "Stamp card with pink glass strip"
      exports: ["StampGlassCard"]
    - path: "src/components/passport/GlassCard/FindGlassCard.tsx"
      provides: "Find card with purple glass strip"
      exports: ["FindGlassCard"]
    - path: "src/components/passport/GlassCard/DiscoveryGlassCard.tsx"
      provides: "Discovery card with blue glass strip"
      exports: ["DiscoveryGlassCard"]
    - path: "src/components/passport/GlassGrid.tsx"
      provides: "2x4 grid layout for glass cards"
      exports: ["GlassGrid"]
  key_links:
    - from: "src/components/passport/OrbBackground.tsx"
      to: "activeTabIndex shared value"
      via: "Reanimated interpolate on orb opacity"
      pattern: "interpolate.*activeTab"
    - from: "src/components/passport/GlassCard/*.tsx"
      to: "src/types/passport.ts"
      via: "CollectionStamp type for card props"
      pattern: "CollectionStamp"
    - from: "src/components/passport/GlassGrid.tsx"
      to: "GlassCard components"
      via: "renders card variant based on collection_type"
      pattern: "StampGlassCard|FindGlassCard|DiscoveryGlassCard"
---

<objective>
Build the visual building blocks: OrbBackground (animated gradient orbs), 3 GlassCard variants (Stamp/Find/Discovery with full-bleed photos and frosted glass strips), and GlassGrid (2x4 layout). These are standalone components wired in Plan 03.

Purpose: All glass card visuals and the immersive orb background, ready to be composed into the passport screen.
Output: 5 new components — OrbBackground, StampGlassCard, FindGlassCard, DiscoveryGlassCard, GlassGrid.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-glassy-passport-redesign/07-CONTEXT.md
@.planning/phases/07-glassy-passport-redesign/07-RESEARCH.md
@.planning/phases/07-glassy-passport-redesign/07-01-SUMMARY.md

@src/types/passport.ts
@src/constants/colors.ts
@src/components/passport/FindCard.tsx

<interfaces>
From src/types/passport.ts (after Plan 01 extends it):
```typescript
export interface PassportTimelineEntry {
  id: string;
  performer: {
    id: string; name: string; slug: string; photo_url: string | null;
    genres: string[]; city: string; platform_url: string | null;
  };
  venue: { name: string } | null;
  event_date: string | null;
  capture_method: "qr" | "nfc" | "location" | "online";
  verified: boolean;
  created_at: string;
  scan_count: number | null;
  current_tier: string | null;
  is_founder?: boolean;
  fan_count: number;
  collection_type?: 'stamp' | 'find' | 'discovery';
  finder_username?: string | null;
  finder_fan_id?: string | null;
}
export interface CollectionStamp extends PassportTimelineEntry {
  rotation: number;
}
```

OrbBackground expected props:
```typescript
interface OrbBackgroundProps {
  activeTabIndex: SharedValue<number>;
}
```

GlassGrid expected props:
```typescript
interface GlassGridProps {
  items: CollectionStamp[];
  type: 'stamp' | 'find' | 'discovery';
  onCardPress: (item: CollectionStamp) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrbBackground component with tab-reactive animated orbs</name>
  <files>src/components/passport/OrbBackground.tsx</files>
  <action>
  Create `src/components/passport/OrbBackground.tsx` with:

  **Props:** `{ activeTabIndex: SharedValue<number> }` — Reanimated shared value (0=Stamps, 1=Finds, 2=Discoveries)

  **Render 3 gradient orbs** as Animated.View + LinearGradient circles:
  - Orb 1 (pink #FF4D6A): positioned top-left area, ~200x200
  - Orb 2 (purple #9B6DFF): positioned center-right area, ~180x180
  - Orb 3 (blue #4D9AFF): positioned bottom-left area, ~160x160

  **Drift animation:** Each orb drifts independently using `withRepeat(withSequence(withTiming(...)))` started in a `useEffect`. Orb 1: 4s X drift, 5s Y drift. Orb 2: 5s X, 6s Y. Orb 3: 6s X, 4.5s Y. Movement range: +/-20px X, +/-15px Y. Easing: `Easing.inOut(Easing.sine)`.

  **Tab-reactive opacity:** Each orb's opacity shifts based on activeTabIndex using `useAnimatedStyle` + `interpolate`:
  - Stamps (0): pink orb bright (0.30), purple dim (0.12), blue dim (0.10) — dark mode values. Light mode: multiply by 0.6.
  - Finds (1): purple orb bright (0.28), pink medium (0.18), blue dim (0.10)
  - Discoveries (2): blue orb bright (0.28), purple medium (0.18), pink dim (0.10)

  **Style:** `StyleSheet.absoluteFillObject`, `pointerEvents: 'none'`, positioned as a sibling BEHIND SafeAreaView (not inside it). Each orb is a LinearGradient with borderRadius: 9999 (circle), no actual BlurView on orbs — perceived blur comes from low opacity + large size + LinearGradient radial falloff.

  Use `useThemeColors()` to detect dark/light mode for opacity multiplier.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>
  - OrbBackground renders 3 gradient circles that drift autonomously
  - Orb opacity shifts smoothly when activeTabIndex changes
  - Component uses StyleSheet.absoluteFillObject and pointerEvents: 'none'
  - Builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Three GlassCard variants + GlassGrid with press-in animation and haptics</name>
  <files>
    src/components/passport/GlassCard/StampGlassCard.tsx
    src/components/passport/GlassCard/FindGlassCard.tsx
    src/components/passport/GlassCard/DiscoveryGlassCard.tsx
    src/components/passport/GlassGrid.tsx
  </files>
  <action>
  **Shared card pattern (all 3 variants):**
  - Full-bleed artist photo fills entire card using `expo-image` Image with `contentFit="cover"`
  - If no photo_url, render a gradient fallback using `getGradientForName(performer.name)` from existing utils
  - Card wrapper: `borderRadius: 16`, `overflow: 'hidden'` on INNER view. Rotation on OUTER Animated.View (Pitfall 5 from research — rotation on outer, overflow on inner to avoid Android clipping bug)
  - Rotation: derive from collection ID hash — `((hashCode(item.id) % 5) - 2)` degrees, deterministic
  - Frosted glass strip at bottom: `<BlurView intensity={40} tint={isDark ? 'dark' : 'light'} blurMethod="dimezisBlurViewSdk31Plus">` positioned absolute bottom. Color tint overlay View with `StyleSheet.absoluteFillObject` and type-specific rgba color at 0.3 opacity
  - **Press-in animation (GPASS-08):** Wrap in Animated.View + Pressable. `onPressIn` → `scale.value = withSpring(0.97, { damping: 15, stiffness: 300 })`. `onPressOut` → `scale.value = withSpring(1.0, { damping: 12, stiffness: 200 })`. `onPress` → `Haptics.impactAsync(ImpactFeedbackStyle.Light)` + `router.push(/artist/${performer.slug})`
  - Card dimensions: `(screenWidth - 48) / 2` width (16px padding each side + 16px gap), height ~1.4x width (portrait ratio)
  - Soft shadow: `Platform.select({ ios: { shadowColor: '#000', shadowOffset: {width:0,height:4}, shadowOpacity: 0.15, shadowRadius: 12 }, android: { elevation: 6 } })`
  - Semi-transparent border: `borderWidth: 1, borderColor: isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.06)'`

  **StampGlassCard (pink tint rgba(255,77,106,0.3)):**
  - Glass strip shows: Artist name (Poppins_600SemiBold, 13px, white), Venue name (Poppins_400Regular, 11px, rgba(255,255,255,0.8)), Date formatted "MMM DD, YYYY" (Poppins_400Regular, 10px, rgba(255,255,255,0.6))
  - If is_founder, show gold star icon (Star from lucide) next to artist name

  **FindGlassCard (purple tint rgba(155,109,255,0.3)):**
  - Glass strip shows: Artist name (same style), Platform icon (detect from platform_url domain — Spotify green circle / SoundCloud orange / Apple Music pink, use small 14px icon), Listener count formatted "XXK listeners" (Poppins_400Regular, 10px)
  - Gold star badge icon always shown (Finds = Founded artists only)

  **DiscoveryGlassCard (blue tint rgba(77,154,255,0.3)):**
  - Glass strip shows: Artist name, Compass icon (Compass from lucide, 14px, purple #9B6DFF), "via @{finder_username}" as a separate tappable Text that navigates to finder profile (`router.push(/user/${finder_fan_id})`)
  - Slightly more transparent: glass strip tint at 0.25 opacity instead of 0.3
  - If finder_username is null, show "via Decibel" as graceful degradation

  **GlassGrid component:**
  - Props: `{ items: CollectionStamp[], type: 'stamp'|'find'|'discovery', onViewMore: () => void }`
  - Renders `items.slice(0, 8)` in a 2-column flexWrap View with 16px gap
  - No empty placeholders — just renders however many items exist (GPASS-13)
  - Below the grid: "View More" button (Pressable, Poppins_600SemiBold, 14px, accent color matching type) — calls onViewMore (GPASS-09)
  - If items.length === 0, render an empty state CTA:
    - Stamps: "Check in at a live show" + button to + tab
    - Finds: "Be the first to find an underground artist" + button to + tab
    - Discoveries: "Discover artists on Decibel" + button to Home
  - Selects correct GlassCard variant based on `type` prop
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx expo export --platform ios 2>&1 | tail -5 && npx expo export --platform android 2>&1 | tail -5</automated>
  </verify>
  <done>
  - 3 GlassCard variants render full-bleed photo + frosted glass strip with correct tint color
  - Press-in scale animation (0.97) + light haptic fires on card tap
  - Card tap navigates to artist profile; Discovery "@finder" navigates to finder profile
  - Cards have slight rotation derived from collection ID
  - GlassGrid renders 2x4 max, no empty placeholders, with View More button
  - Empty state CTAs render when items.length === 0
  - Both platform exports succeed
  </done>
</task>

</tasks>

<verification>
- `npx expo export --platform ios` and `--platform android` succeed
- All 5 new component files exist and export their named components
- No hardcoded colors — all use useThemeColors()
- Rotation wrapper is separate from overflow:hidden wrapper (Android compatibility)
</verification>

<success_criteria>
- OrbBackground animates 3 orbs with tab-reactive opacity shifting
- 3 GlassCard variants show correct content per type with frosted glass strips
- GlassGrid handles 0-8 items gracefully without placeholders
- Press-in + haptic interaction works on all cards
- Both iOS and Android exports build cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-glassy-passport-redesign/07-02-SUMMARY.md`
</output>
