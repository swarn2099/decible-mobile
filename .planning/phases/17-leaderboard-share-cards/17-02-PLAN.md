---
phase: 17-leaderboard-share-cards
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - /home/swarn/decibel/src/app/api/share-card/founder/route.tsx
  - /home/swarn/decibel/src/app/api/share-card/passport/route.tsx
  - /home/swarn/decibel-mobile/src/hooks/useShareCard.ts
  - /home/swarn/decibel-mobile/app/(tabs)/add.tsx
  - /home/swarn/decibel-mobile/app/(tabs)/passport.tsx
  - /home/swarn/decibel-mobile/src/components/passport/ShareSheet.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03]

must_haves:
  truths:
    - "Founder Share Card generates on founding with artist image, 'FOUNDED BY @username', artist name, listener count, date, and Decibel branding"
    - "Passport Summary Card generates on demand with avatar, username, Finds/Founders/Influence stats, top 3 founded artist images, and Decibel branding"
    - "Share sheet opens with the generated card image and targets Instagram Stories"
  artifacts:
    - path: "/home/swarn/decibel/src/app/api/share-card/founder/route.tsx"
      provides: "1080x1920 founder card with listener count param"
    - path: "/home/swarn/decibel/src/app/api/share-card/passport/route.tsx"
      provides: "1080x1920 passport summary card with v6.0 stats"
    - path: "/home/swarn/decibel-mobile/src/hooks/useShareCard.ts"
      provides: "Updated hooks passing correct v6.0 params"
  key_links:
    - from: "app/(tabs)/add.tsx"
      to: "/api/share-card/founder"
      via: "useFounderShareCard -> download"
      pattern: "founderShareCard.*generate"
    - from: "app/(tabs)/passport.tsx"
      to: "/api/share-card/passport"
      via: "usePassportShareCardV2 -> download"
      pattern: "passportShare.*generate"
    - from: "hooks/useShareCard.ts"
      to: "Share.share"
      via: "native share sheet"
      pattern: "Share\\.share"
---

<objective>
Update the server-rendered share card APIs to match v6.0 stats and design, then wire the mobile hooks to pass correct parameters and open the share sheet targeting Instagram Stories.

Purpose: Share cards are the viral loop — they need to look premium (Spotify Wrapped tier) and carry the right data for v6.0 (Finds, Founders, Influence Score instead of old show/venue stats).
Output: Working founder and passport share cards that generate correctly and open in the share sheet.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-leaderboard-share-cards/17-CONTEXT.md

<interfaces>
<!-- Existing founder card API -->
From /home/swarn/decibel/src/app/api/share-card/founder/route.tsx:
- Edge runtime, uses `ImageResponse` from next/og
- Params: artistName, artistPhoto, fanSlug
- Returns 1080x1920 PNG with dark bg, artist initials/photo, "FOUNDED BY" text

<!-- Existing passport card API -->
From /home/swarn/decibel/src/app/api/share-card/passport/route.tsx:
- Edge runtime, uses `ImageResponse`
- Params: name, artistsFound, showsAttended, venues, topPhotos
- Returns PNG with stats grid

<!-- Mobile hooks -->
From /home/swarn/decibel-mobile/src/hooks/useShareCard.ts:
- useFounderShareCard: generates({ artistName, artistPhoto, fanSlug })
- usePassportShareCardV2: generates({ name, artistsFound, showsAttended, venues, topPhotos })
- Both download server-generated PNG to cache, return URI

<!-- Add flow integration -->
From /home/swarn/decibel-mobile/app/(tabs)/add.tsx:
- On founding, calls `founderShareCard.generate({ artistName, artistPhoto, fanSlug })`
- ShareSheet component receives imageUri and opens Share.share()

<!-- Passport integration -->
From /home/swarn/decibel-mobile/app/(tabs)/passport.tsx:
- PassportHeader onSharePress calls handleSharePassport
- Uses usePassportShareCardV2 to generate card then opens ShareSheet

From /home/swarn/decibel-mobile/src/components/passport/ShareSheet.tsx:
- Receives imageUri, calls Share.share() with the image
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update share card APIs for v6.0 stats and premium design</name>
  <files>
/home/swarn/decibel/src/app/api/share-card/founder/route.tsx
/home/swarn/decibel/src/app/api/share-card/passport/route.tsx
  </files>
  <action>
**Founder Share Card (`/api/share-card/founder/route.tsx`):**

Add new query params:
- `listenerCount` (string, optional) — Spotify monthly listeners at time of find (e.g., "42K")
- `foundDate` (string, optional) — date of find (e.g., "Mar 16, 2026")

Redesign the ImageResponse JSX for premium quality (1080x1920):
- Full-bleed artist photo as background (if `artistPhoto` provided), with a dark gradient overlay from bottom (transparent at top -> #0B0B0F at bottom, ~60% of height)
- If no photo, use dark gradient background (#0B0B0F -> #1a1a2e)
- Top section: Small Decibel logo or "DECIBEL" wordmark in subtle white at top-center
- Center-bottom content (over the gradient):
  - "FOUNDED BY" in uppercase, letter-spaced, white, 24px
  - `@${fanSlug}` in bold white, 40px
  - Artist name in bold white, 56px
  - If listenerCount provided: "Found at ${listenerCount} listeners" in pink (#FF4D6A), 28px
  - Date in muted gray, 24px
- Bottom: "decibel.live" URL in small text, bottom-center

Keep `export const runtime = "edge"` and `ImageResponse` pattern. Size: `{ width: 1080, height: 1920 }`.

**Passport Summary Card (`/api/share-card/passport/route.tsx`):**

Update query params for v6.0:
- `name` (username)
- `finds` (number of finds)
- `founders` (number of founders)
- `influence` (influence score)
- `topPhotos` (comma-separated artist photo URLs, up to 3)
- `avatarUrl` (optional, user's avatar)

Remove old params (artistsFound, showsAttended, venues).

Redesign the ImageResponse JSX (1080x1920):
- Dark background (#0B0B0F)
- Top section: "DECIBEL" wordmark, subtle
- Center: User avatar (circular, 200px) or initial letter with gradient bg
- Username below avatar, bold white, 48px
- Stats row: three stat blocks side by side
  - "X Finds" | "X Founders" | "X Influence"
  - Numbers in bold white 56px, labels in muted 24px
  - Founders count in gold (#FFD700)
- Below stats: "Top Founded Artists" label
  - Row of up to 3 circular artist photos (120px each) with subtle border
  - If fewer than 3, show what's available
- Bottom: "decibel.live/@username" in muted text + Decibel branding

Size: `{ width: 1080, height: 1920 }`.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx tsc --noEmit src/app/api/share-card/founder/route.tsx src/app/api/share-card/passport/route.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Both share card APIs accept v6.0 params and render premium 1080x1920 cards via ImageResponse.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mobile hooks + share sheet for v6.0 params and Instagram Stories</name>
  <files>
/home/swarn/decibel-mobile/src/hooks/useShareCard.ts
/home/swarn/decibel-mobile/app/(tabs)/add.tsx
/home/swarn/decibel-mobile/app/(tabs)/passport.tsx
/home/swarn/decibel-mobile/src/components/passport/ShareSheet.tsx
  </files>
  <action>
**1. Update `useShareCard.ts`:**

Update `useFounderShareCard` params type to include optional `listenerCount` and `foundDate`:
```typescript
type FounderShareParams = {
  artistName: string;
  artistPhoto: string | null;
  fanSlug: string;
  listenerCount?: string; // e.g., "42K"
  foundDate?: string;     // e.g., "Mar 16, 2026"
};
```
Pass these as query params when building the URL.

Update `usePassportShareCardV2` params type for v6.0:
```typescript
type PassportShareV2Params = {
  name: string;
  finds: number;
  founders: number;
  influence: number;
  topPhotos: string[];
  avatarUrl?: string;
};
```
Build URL with `finds`, `founders`, `influence` instead of old `artistsFound`, `showsAttended`, `venues`.

**2. Update `app/(tabs)/add.tsx`:**

When generating the founder share card after a successful founding, pass the additional params:
- `listenerCount`: Format the artist's spotify_monthly_listeners or soundcloud_followers from the validation result. Use a helper to format: < 1000 -> exact number, >= 1000 -> "XK", >= 1M -> "X.XM". This data should be available from the artist preview (the validation response includes listener/follower counts).
- `foundDate`: `new Date().toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })`

**3. Update `app/(tabs)/passport.tsx`:**

In `handleSharePassport`, update the `passportShare.generate()` call to pass v6.0 params:
- `name`: fanProfile display name or email prefix
- `finds`: finds.length
- `founders`: founders.length
- `influence`: compute from userStats hook if available, or pass 0
- `topPhotos`: take the first 3 founders' photo URLs (from the founders array)
- `avatarUrl`: fanProfile.avatar_url

**4. Update ShareSheet to target Instagram Stories:**

In `ShareSheet.tsx`, when calling `Share.share()`, include the image URI. The standard React Native `Share.share()` API doesn't directly target Instagram Stories, but passing `{ url: localFileUri }` on iOS will show Instagram Stories as an option in the share sheet if Instagram is installed.

Ensure the share call uses:
```typescript
await Share.share({
  url: imageUri,  // local file URI from view-shot/download
  message: "Check out my Decibel passport! decibel.live",
});
```

For the founder share card flow in add.tsx, ensure the existing SharePrompt or ShareSheet component receives the generated founder card URI and opens share with it. The existing flow already does this — just verify it passes the new URI correctly.

Note: The existing 300ms setTimeout before Share.share() (to avoid iOS modal conflicts) must be preserved. Do NOT change this workaround — it's a known iOS bug fix from Phase 14.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Founder share card passes listener count + date. Passport share card passes v6.0 stats (Finds, Founders, Influence). Share sheet opens with card image. Instagram Stories appears as a share target when Instagram is installed.</done>
</task>

</tasks>

<verification>
1. `cd /home/swarn/decibel && npx tsc --noEmit` — API compiles
2. `cd /home/swarn/decibel-mobile && npx tsc --noEmit` — mobile compiles
3. Deploy API: `cd /home/swarn/decibel && vercel --prod`
4. Test founder card URL in browser: `https://decibel-three.vercel.app/api/share-card/founder?artistName=TestArtist&fanSlug=swarn&listenerCount=42K&foundDate=Mar+16+2026` — returns a 1080x1920 PNG
5. Test passport card URL: `https://decibel-three.vercel.app/api/share-card/passport?name=Swarn&finds=12&founders=5&influence=89` — returns a 1080x1920 PNG
6. Deploy mobile and verify: found an artist -> share card shows with listener count, passport share -> card shows v6.0 stats
</verification>

<success_criteria>
- Founder Share Card (1080x1920) generates on founding with artist image, @username, artist name, listener count, date, and Decibel branding
- Passport Summary Card (1080x1920) generates on demand with user avatar, username, Finds/Founders/Influence stats, top 3 founded artist photos, and Decibel branding
- Share sheet opens with card image; Instagram Stories is available as a share target
- Both cards render correctly when viewed as URLs in browser
</success_criteria>

<output>
After completion, create `.planning/phases/17-leaderboard-share-cards/17-02-SUMMARY.md`
</output>
