---
phase: 10-login-flow-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/login.tsx
  - src/components/auth/LoginOrbBackground.tsx
autonomous: true
requirements:
  - LOGIN-01
  - LOGIN-02
  - LOGIN-03
  - LOGIN-04
  - LOGIN-05
  - LOGIN-06
  - LOGIN-07
  - LOGIN-08

must_haves:
  truths:
    - "Login screen uses dark bg (#0B0B0F) in dark mode and light bg (#F5F5F7) in light mode"
    - "Three gradient orbs (pink/purple/blue) float behind content — subtler in light mode (0.6x opacity)"
    - "Wordmark 'D E C I B E L' and tagline fade in from top, then input and button stagger in below"
    - "Email input has mail icon, pink border glow on focus"
    - "Gradient button (pink->purple) has press-scale animation + haptic feedback"
    - "Button shows spinner while loading, checkmark + 'Check your email' on success"
    - "Keyboard slides up without obscuring input or button on both iOS and Android"
  artifacts:
    - path: "src/components/auth/LoginOrbBackground.tsx"
      provides: "Reanimated floating orbs for login screen with theme-aware opacity"
      min_lines: 60
    - path: "app/(auth)/login.tsx"
      provides: "Theme-aware login screen with stagger animations, magic-link flow, keyboard avoidance"
      min_lines: 200
  key_links:
    - from: "app/(auth)/login.tsx"
      to: "src/constants/colors.ts"
      via: "useThemeColors hook"
      pattern: "useThemeColors"
    - from: "app/(auth)/login.tsx"
      to: "src/components/auth/LoginOrbBackground.tsx"
      via: "component import"
      pattern: "LoginOrbBackground"
    - from: "app/(auth)/login.tsx"
      to: "src/lib/supabase.ts"
      via: "signInWithOtp call"
      pattern: "signInWithOtp"
---

<objective>
Redesign the login screen to be fully theme-aware (dark/light mode), upgrade the inline orb animations to use Reanimated (matching the established OrbBackground pattern from Phase 7), and ensure keyboard avoidance works on both iOS and Android.

Purpose: The login screen is the first impression of Decibel — it must feel polished, branded, and adapt to the user's device theme preference. Currently it's hardcoded to dark mode with basic RN Animated orbs.

Output: A theme-aware login screen with Reanimated gradient orbs, stagger-fade entrance animation, and smooth keyboard handling.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@app/(auth)/login.tsx
@src/components/passport/OrbBackground.tsx
@src/constants/colors.ts

<interfaces>
<!-- Theme system — use useThemeColors() for all color values -->

From src/constants/colors.ts:
```typescript
export function useThemeColors(): ThemeColors;
// Key fields: bg, card, inputBg, inputBorder, text, textPrimary, textSecondary,
// textTertiary, gray, pink, purple, blue, isDark
```

From src/components/passport/OrbBackground.tsx:
```typescript
// Pattern: Reanimated SharedValues + LinearGradient for each orb
// withRepeat + withSequence + withTiming for drift animation
// useAnimatedStyle for opacity + transform
// Easing.inOut(Easing.sin) for smooth oscillation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LoginOrbBackground component with Reanimated</name>
  <files>src/components/auth/LoginOrbBackground.tsx</files>
  <action>
Create a standalone LoginOrbBackground component that renders three floating gradient orbs (pink, purple, blue) using Reanimated — following the exact same pattern as src/components/passport/OrbBackground.tsx but simpler (no activeTabIndex dependency).

Key differences from passport OrbBackground:
- No `activeTabIndex` prop — static opacity values instead
- Accept `isDark` boolean prop to control opacity multiplier
- Dark mode: orb opacity ~0.18 (current value). Light mode: orb opacity ~0.10 (subtler)
- Use the same Reanimated drift pattern: `withRepeat(withSequence(withTiming(...)))` with `Easing.inOut(Easing.sin)`
- Three orbs: pink (#FF4D6A) top-left area, purple (#9B6DFF) center-right area, blue (#4D9AFF) bottom-center area
- Orb sizes: 280, 320, 240 (matching current login.tsx GradientOrb sizes)
- Each orb is an Animated.View containing a LinearGradient with radial falloff (color -> transparent)
- Component is positioned absolute, covers full screen, pointerEvents="none"

Props interface:
```typescript
interface LoginOrbBackgroundProps {
  isDark: boolean;
}
```

Do NOT use BlurView. Do NOT import from the passport OrbBackground — this is a separate component to avoid coupling.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit src/components/auth/LoginOrbBackground.tsx 2>&1 | head -20</automated>
  </verify>
  <done>LoginOrbBackground.tsx exists, exports the component, TypeScript compiles without errors, uses Reanimated for drift animations and theme-aware opacity</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite login.tsx with theme support, new orbs, and keyboard handling</name>
  <files>app/(auth)/login.tsx</files>
  <action>
Rewrite login.tsx to be fully theme-aware while preserving ALL existing functionality (magic link flow, review email bypass, verify navigation, stagger animations, haptics).

Changes required:

1. **Theme integration (LOGIN-01):**
   - Import and use `useThemeColors()` from `@/constants/colors`
   - Root View backgroundColor: `colors.bg` (not hardcoded "#0B0B0F")
   - StatusBar barStyle: `colors.isDark ? "light-content" : "dark-content"`
   - Wordmark text color: `colors.textPrimary`
   - Tagline text color: `colors.textSecondary`
   - Footer/helper text color: `colors.gray`
   - Input bg: `colors.inputBg` (not hardcoded "#1A1A1F")
   - Input border default: `colors.inputBorder` (not hardcoded "rgba(255,255,255,0.12)")
   - Input text color: `colors.textPrimary`
   - Input placeholder color: `colors.gray`
   - Mail icon color: `colors.gray`
   - Error text color: `colors.pink`
   - "Terms" link color: `colors.pink`

2. **Orb background (LOGIN-02):**
   - Remove the inline `GradientOrb` component entirely
   - Import `LoginOrbBackground` from `@/components/auth/LoginOrbBackground`
   - Render `<LoginOrbBackground isDark={colors.isDark} />` as first child of root View, positioned absolute

3. **Email input (LOGIN-04):**
   - Keep the mail icon, focus state with pink border glow, error state with pink border
   - Focus glow: shadowColor `colors.pink`, same shadow values
   - All colors from theme tokens as listed above

4. **Gradient button (LOGIN-05):**
   - Keep LinearGradient `["#FF4D6A", "#9B6DFF"]` for normal state (brand gradient, same in both themes)
   - Keep `["#22C55E", "#16A34A"]` for sent state (green success, same in both themes)
   - Keep press-scale spring animation (buttonScale) with `Haptics.impactAsync(Medium)` on press
   - Button text stays white in both themes (white on gradient is always readable)

5. **Loading and success states (LOGIN-06):**
   - Keep ActivityIndicator (white) during loading
   - Keep Check icon + "Check your email" text on success
   - Keep the 1200ms delay before navigating to verify screen

6. **Stagger animation (LOGIN-07):**
   - Keep existing RNAnimated.stagger pattern — it works well
   - Sequence: wordmark (fade+slide) -> tagline (fade) -> input (fade+slide) -> button (fade+slide) -> footer (fade)
   - Keep same timing values (150ms stagger, 300-400ms per item)

7. **Keyboard avoidance (LOGIN-08):**
   - Keep KeyboardAvoidingView with `behavior={Platform.OS === "ios" ? "padding" : "height"}`
   - Keep ScrollView with `keyboardShouldPersistTaps="handled"` and `contentContainerStyle={{ flexGrow: 1, justifyContent: "center" }}`
   - This pattern already works — do not change it

Preserve the REVIEW_EMAIL constant and review mode bypass. Preserve the verify screen navigation with email param. Preserve all error handling.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit app/\(auth\)/login.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Login screen renders correctly with theme colors from useThemeColors(), uses LoginOrbBackground component, all stagger animations fire on mount, keyboard avoidance works, magic link flow sends OTP and shows spinner then checkmark</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for both files without errors
2. `npx expo export --platform ios` completes without build errors
3. Visual check: login screen shows dark bg with bright orbs in dark mode, light bg with subtle orbs in light mode
4. Visual check: wordmark, tagline, input, button all stagger-fade in on screen mount
5. Functional check: entering email and tapping button shows spinner, then checkmark + "Check your email"
6. Keyboard check: tapping email field raises keyboard without hiding input or button
</verification>

<success_criteria>
- Login screen adapts to dark/light mode with all colors from useThemeColors()
- Gradient orbs use Reanimated (not RN Animated) and are dimmer in light mode
- Stagger entrance animation plays on mount (wordmark -> tagline -> input -> button)
- Email input shows pink glow on focus, pink border on error
- Button press triggers haptic + scale animation, shows spinner then checkmark on success
- Keyboard avoidance keeps input and button visible on both iOS and Android
</success_criteria>

<output>
After completion, create `.planning/phases/10-login-flow-redesign/10-01-SUMMARY.md`
</output>
