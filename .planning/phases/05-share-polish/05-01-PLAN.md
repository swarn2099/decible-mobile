---
phase: 05-share-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/decibel/src/app/api/share-card/founder/route.tsx
  - ~/decibel/src/app/api/share-card/passport/route.tsx
autonomous: true
requirements: [SHR-03, SHR-04]

must_haves:
  truths:
    - "GET /api/share-card/founder returns a 1080x1920 PNG with artist photo, artist name, 'FOUNDED BY [username]' in gold, and Decibel branding on dark background"
    - "GET /api/share-card/passport returns a 1080x1920 PNG with user stats, top artist photo grid, and Decibel branding on dark background"
    - "Both endpoints handle missing artist photos gracefully (initials fallback in gradient circle)"
  artifacts:
    - path: "~/decibel/src/app/api/share-card/founder/route.tsx"
      provides: "Founder share card PNG generation"
      exports: ["GET"]
    - path: "~/decibel/src/app/api/share-card/passport/route.tsx"
      provides: "Passport share card PNG generation"
      exports: ["GET"]
  key_links:
    - from: "~/decibel/src/app/api/share-card/founder/route.tsx"
      to: "next/og ImageResponse"
      via: "Edge runtime PNG render"
      pattern: "ImageResponse"
    - from: "~/decibel/src/app/api/share-card/passport/route.tsx"
      to: "next/og ImageResponse"
      via: "Edge runtime PNG render"
      pattern: "ImageResponse"
---

<objective>
Create two server-side share card generation routes using next/og ImageResponse: one for Founder cards (artist-focused, 9:16 Stories format) and one for Passport cards (stats + artist grid). Both always render on dark background (#0B0B0F) regardless of user theme.

Purpose: Backend PNG generation must exist before mobile can fetch and share cards.
Output: Two deployed Vercel Edge routes returning image/png at /api/share-card/founder and /api/share-card/passport.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-share-polish/05-CONTEXT.md
@.planning/phases/05-share-polish/05-RESEARCH.md

<interfaces>
<!-- Existing share card pattern to follow exactly -->

From ~/decibel/src/app/api/passport/share-card/route.tsx:
```typescript
import { ImageResponse } from "next/og";
export const runtime = "edge";
export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  // ... read params ...
  return new ImageResponse(
    (<div style={{ display: "flex", ... }}>...</div>),
    { width: 1080, height: 1920 }  // 9:16 Stories format
  );
}
```

Key Satori constraints:
- Every flex container MUST have `display: "flex"` inline
- Images MUST be absolute HTTPS URLs (Supabase CDN OK)
- No Tailwind, no external CSS, inline styles only
- No custom fonts for v1 (system default via fontWeight)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/share-card/founder route</name>
  <files>~/decibel/src/app/api/share-card/founder/route.tsx</files>
  <action>
Create the Founder share card Edge route. Follow the exact pattern from ~/decibel/src/app/api/passport/share-card/route.tsx (read it first).

Query params: `artistName` (string), `artistPhoto` (optional HTTPS URL), `fanSlug` (string, the founder's username).

Layout (1080x1920, always #0B0B0F background):
1. Top accent: thin gradient bar (pink #FF4D6A to purple #9B6DFF), 4px height, full width
2. Artist photo (top 60% of card): if `artistPhoto` exists, render as `<img>` covering the area with object-fit cover. If missing, render a 300x300 circle with gradient background (#FF4D6A to #9B6DFF) containing artist initials (first letter of each word in artistName, max 2 chars) in white, 120px font
3. Artist name: centered below photo, white text, 56px font weight 700
4. "FOUNDED BY" label: centered, gold (#FFD700) text, 28px, uppercase, letter-spacing 4px
5. Founder username: centered, gold (#FFD700) text, 44px font weight 700, uppercase
6. Bottom branding: "DECIBEL" text in white at 24px, 60px from bottom, with opacity 0.4

Every `<div>` that acts as a flex container MUST have `display: "flex"` as a style property. Use `export const runtime = "edge"`.

Deploy: `cd ~/decibel && vercel --prod` (or note for batch deploy with Task 2).
  </action>
  <verify>
    <automated>cd ~/decibel && npx tsc --noEmit src/app/api/share-card/founder/route.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Route file exists, TypeScript compiles, deployed to Vercel. `curl -s -o /dev/null -w "%{http_code}" "https://decible.live/api/share-card/founder?artistName=Test&fanSlug=testuser"` returns 200.</done>
</task>

<task type="auto">
  <name>Task 2: Create /api/share-card/passport route</name>
  <files>~/decibel/src/app/api/share-card/passport/route.tsx</files>
  <action>
Create the Passport share card Edge route. Same pattern as Task 1.

Query params: `name` (user display name), `artistsFound` (number), `showsAttended` (number), `venues` (number), `topPhotos` (comma-separated HTTPS URLs, up to 4).

Layout (1080x1920, always #0B0B0F background):
1. Top accent: same gradient bar as founder card
2. User name: centered, white, 48px font weight 700, ~120px from top
3. "PASSPORT" label: centered below name, white opacity 0.5, 24px, uppercase, letter-spacing 6px
4. Stats row: three stat blocks side by side — each has a large number (64px, white, bold) over a label (20px, white opacity 0.6). Stats: "[N] Artists" / "[N] Shows" / "[N] Venues"
5. 2x2 photo grid: centered, 400x400 total area. Each cell ~190x190 with 10px gap. For each `topPhotos` URL render an `<img>` with border-radius 12px. Empty cells: render placeholder circle with gradient (#FF4D6A/#9B6DFF) at 0.3 opacity
6. Bottom branding: same as founder card

Deploy both routes: `cd ~/decibel && vercel --prod`. Verify both return 200 with `content-type: image/png`.
  </action>
  <verify>
    <automated>cd ~/decibel && npx tsc --noEmit src/app/api/share-card/passport/route.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Route file exists, TypeScript compiles, deployed to Vercel. Both `/api/share-card/founder` and `/api/share-card/passport` return 200 with content-type image/png.</done>
</task>

</tasks>

<verification>
After deploying both routes:
```bash
curl -s -o /dev/null -w "%{http_code} %{content_type}" "https://decible.live/api/share-card/founder?artistName=TestArtist&fanSlug=testfan"
curl -s -o /dev/null -w "%{http_code} %{content_type}" "https://decible.live/api/share-card/passport?name=TestUser&artistsFound=5&showsAttended=3&venues=2"
```
Both should return `200 image/png`.
</verification>

<success_criteria>
- Two new Edge routes exist in ~/decibel/src/app/api/share-card/
- Both return valid PNG images at 1080x1920
- Founder card shows artist photo (or initials), "FOUNDED BY [username]" in gold
- Passport card shows stats and photo grid
- Both handle missing photos gracefully
- Both deployed to Vercel production
</success_criteria>

<output>
After completion, create `.planning/phases/05-share-polish/05-01-SUMMARY.md`
</output>
