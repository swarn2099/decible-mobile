---
phase: 15-passport-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/passport/PassportHeader.tsx
  - app/(auth)/login.tsx
  - src/components/auth/LoginOrbBackground.tsx
autonomous: true
requirements: [PASS-01, PASS-02, PASS-05, PASS-06]

must_haves:
  truths:
    - "Login screen shows animated orbs, branded wordmark, Poppins input/button in both dark and light mode"
    - "Passport header matches Instagram compact layout: avatar (no colored ring), 4 inline stats, username, member since"
    - "Share Passport and Edit Profile are side-by-side text buttons below the stats, not icon circles"
    - "No settings gear, no badge teaser, no colored avatar ring anywhere on the passport screen"
  artifacts:
    - path: "src/components/passport/PassportHeader.tsx"
      provides: "Instagram-style header with text action buttons"
      contains: "Share Passport"
    - path: "app/(auth)/login.tsx"
      provides: "Login screen with orbs, branded input, animations"
      contains: "LoginOrbBackground"
    - path: "src/components/auth/LoginOrbBackground.tsx"
      provides: "Animated gradient orbs background"
      contains: "opacityMultiplier"
  key_links:
    - from: "app/(auth)/login.tsx"
      to: "src/components/auth/LoginOrbBackground.tsx"
      via: "import"
      pattern: "LoginOrbBackground"
    - from: "app/(tabs)/passport.tsx"
      to: "src/components/passport/PassportHeader.tsx"
      via: "import"
      pattern: "PassportHeader"
---

<objective>
Polish the login screen and passport header to match the v6.0 spec exactly.

Purpose: The login screen already has orbs, branded input, and animations from Phase 10 -- verify it matches spec and fix any light/dark mode issues. The passport header has the right layout from Phase 11/14 but uses icon circles for Share/Edit instead of the specified side-by-side text buttons ("Share Passport" gradient fill + "Edit Profile" surface fill). Fix the header buttons and confirm no settings gear, badge teaser, or colored avatar ring exists.

Output: Polished login screen and passport header matching the Instagram-profile spec exactly.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-passport-redesign/15-CONTEXT.md

@app/(auth)/login.tsx
@src/components/auth/LoginOrbBackground.tsx
@src/components/passport/PassportHeader.tsx
@app/(tabs)/passport.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/passport/PassportHeader.tsx:
```typescript
type Props = {
  displayName: string | null;
  avatarUrl: string | null;
  memberSince: string;
  followersCount: number;
  followingCount: number;
  findsCount: number;
  foundersCount: number;
  fanId: string;
  onSharePress?: () => void;
  isSharing?: boolean;
};
```

From src/constants/colors.ts (theme hook):
```typescript
// useThemeColors() returns: bg, card, cardBorder, text, textPrimary, textSecondary, textTertiary, pink, purple, gold, gray, inputBg, inputBorder, isDark
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign PassportHeader action buttons from icon circles to text buttons</name>
  <files>src/components/passport/PassportHeader.tsx</files>
  <action>
Replace the current icon-circle buttons (Share2 gradient circle + UserPen surface circle) with two side-by-side text buttons matching Instagram's profile pattern:

1. "Share Passport" button: LinearGradient fill (pink to purple), white text, Poppins_600SemiBold 13px, height 34px, borderRadius 8, flex 1
2. "Edit Profile" button: surface fill (colors.card), themed border (colors.cardBorder), themed text (colors.text), Poppins_600SemiBold 13px, height 34px, borderRadius 8, flex 1
3. Wrap both in a horizontal row with gap 8, marginTop 12, below the username/member-since row
4. Remove the icon button View (Share2 gradient circle + UserPen surface circle) from the right side of the username row
5. Keep the press animation (scale spring) on both buttons
6. Keep haptic feedback on both button presses
7. "Edit Profile" navigates to /settings (same as current edit button)
8. "Share Passport" calls onSharePress (same as current share button)

Verify no settings gear icon, no badge teaser, no colored avatar ring exists anywhere in PassportHeader. The avatar should have only a 1px cardBorder ring (already correct).
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>PassportHeader shows "Share Passport" (gradient) and "Edit Profile" (surface) as side-by-side text buttons. No icon circles. No settings gear. No badge teaser. No colored avatar ring. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify login screen and audit light/dark mode on both screens</name>
  <files>app/(auth)/login.tsx, src/components/auth/LoginOrbBackground.tsx</files>
  <action>
Verify the login screen matches the spec. It should already be correct from Phase 10. Check these specific items:

1. LoginOrbBackground: orbs have lower opacity in light mode (opacityMultiplier = 0.6 for light vs 1.0 for dark) -- ALREADY CORRECT, no change needed
2. Wordmark "D E C I B E L" in Poppins_700Bold, letterSpacing 8 -- ALREADY CORRECT
3. Tagline "Your Live Music Passport" -- ALREADY CORRECT
4. Email input with pink border glow on focus -- ALREADY CORRECT
5. Gradient button (pink to purple) "Send Magic Link" -- ALREADY CORRECT
6. Stagger fade-in animations -- ALREADY CORRECT
7. Success state (green gradient + checkmark + "Check your email") -- ALREADY CORRECT

If everything is already correct, make NO changes to the login files. Only fix if something doesn't match the spec.

Then do a theme audit across both screens:
- Ensure all Text colors use theme tokens (colors.text, colors.textPrimary, colors.textSecondary, colors.textTertiary), not hardcoded values
- Ensure all backgrounds use theme tokens (colors.bg, colors.card)
- Check that the PassportHeader StatCell uses colors.text (not colors.textPrimary) consistently
- The grid overlay text (white on dark frost) is acceptable since it's always on top of an image

Report findings. Only modify files if actual issues are found.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>Login screen matches spec (animated orbs, branded Poppins input/button, stagger animations, dark/light mode). No theme-token issues found in login or passport header. Both screens work correctly in dark and light mode.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- PassportHeader renders side-by-side text buttons ("Share Passport" + "Edit Profile")
- No icon circles remain for Share/Edit
- Login screen has animated orbs background with theme-aware opacity
- No settings gear, badge teaser, or colored avatar ring on passport
- All colors from theme tokens, not hardcoded
</verification>

<success_criteria>
1. PassportHeader action buttons are "Share Passport" (gradient fill) and "Edit Profile" (surface fill), side by side, full width
2. Login screen has animated orbs, branded input/button, stagger animations in both themes
3. No settings gear, badge teaser, or colored avatar ring anywhere on passport screen
4. All theme colors come from useThemeColors(), not hardcoded values
</success_criteria>

<output>
After completion, create `.planning/phases/15-passport-redesign/15-01-SUMMARY.md`
</output>
