---
phase: 15-passport-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/passport/GlassGrid.tsx
  - src/components/passport/PassportPager.tsx
autonomous: true
requirements: [PASS-03, PASS-04]

must_haves:
  truths:
    - "Grid cells are square (1:1 aspect ratio) with ~1px gaps between all cells"
    - "Finds cells show artist name + platform icon + date in the bottom overlay"
    - "Founders cells show gold star badge in top-right corner"
    - "Discoveries cells show artist name + via @username + date"
    - "Sticky tab bar pins to top with 4 tabs (Finds/Founders/Discoveries/Badges)"
    - "Swipe gestures switch between all 4 tabs with smooth pink underline animation"
  artifacts:
    - path: "src/components/passport/GlassGrid.tsx"
      provides: "3-column square grid with 1px gaps and correct overlays"
      contains: "CELL_GAP"
    - path: "src/components/passport/PassportPager.tsx"
      provides: "4-tab pager with sticky tab bar and swipe"
      contains: "PagerView"
  key_links:
    - from: "src/components/passport/PassportPager.tsx"
      to: "src/components/passport/GlassGrid.tsx"
      via: "import CollectionGrid"
      pattern: "CollectionGrid"
    - from: "app/(tabs)/passport.tsx"
      to: "src/components/passport/PassportPager.tsx"
      via: "import PassportPager"
      pattern: "PassportPager"
---

<objective>
Fix the grid cell dimensions and gaps to match the Instagram-style 1:1 square spec, and verify the tab bar behavior.

Purpose: The current grid has cells at 1:1.25 aspect ratio (not square) with 4px gaps (spec says ~1px). The tab bar and swipe gestures are already working from Phase 11/14 but need verification. Fix the grid dimensions and gaps to create the dense Instagram-style grid look.

Output: Square 1:1 grid cells with 1px gaps, correct overlays per tab type, and verified sticky tab bar with swipe.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-passport-redesign/15-CONTEXT.md

@src/components/passport/GlassGrid.tsx
@src/components/passport/PassportPager.tsx
@app/(tabs)/passport.tsx

<interfaces>
<!-- Key types the executor needs -->

From src/types/passport.ts:
```typescript
export interface CollectionStamp {
  id: string;
  collection_type: string;
  created_at: string;
  is_founder: boolean;
  finder_username?: string;
  performer: {
    name: string;
    slug: string;
    photo_url: string | null;
    platform_url: string | null;
  };
  venue?: {
    name: string;
  };
}
```

From src/types/badges.ts:
```typescript
export type BadgeRarity = "common" | "rare" | "epic" | "legendary";
export interface BadgeWithStatus {
  id: string;
  name: string;
  icon: string;
  rarity: BadgeRarity;
  earned: boolean;
  earned_at?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix grid cells to 1:1 square aspect ratio with 1px gaps</name>
  <files>src/components/passport/GlassGrid.tsx</files>
  <action>
Make these specific changes to GlassGrid.tsx:

1. Change CELL_GAP from 4 to 1 (the spec says "uniform 1px gap on all sides")
2. In GridCell, change cell height from `cellSize * 1.25` to `cellSize` (making it a perfect 1:1 square)
3. Adjust the frost overlay: since cells are now shorter, the frost section should be ~40% height (up from 35%) to fit the 3 lines of text in a smaller cell. Increase font sizes slightly or reduce padding to keep text readable.
4. Recalculate cellSize: `(screenWidth - CELL_GAP * (COLUMNS - 1)) / COLUMNS` -- with CELL_GAP=1 this means cells take up almost the full width
5. Verify the frost overlay content still shows all 3 lines:
   - Line 1: Artist name (Poppins_600SemiBold, 11px)
   - Line 2: Context (platform for finds, "via @user" for discoveries, venue for stamps)
   - Line 3: Date
6. Keep the BlurView frost effect, founder badge (gold star top-right), haptic feedback, press-down scale animation, and empty states exactly as they are
7. Keep paddingHorizontal 6 on frostContent (spec says ~8px left padding, 6 is close enough)

Do NOT change: gradient fallback colors, platform detection, empty states, router navigation, animations.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>Grid cells are 1:1 square. Gaps are 1px. 3 lines of overlay text still visible and readable. Founder badge gold star in top-right. Press animation + haptics work.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tab bar stickiness and swipe gestures</name>
  <files>src/components/passport/PassportPager.tsx</files>
  <action>
Verify PassportPager matches spec. Check these items:

1. Tab bar has 4 tabs: Finds, Founders, Discoveries, Badges -- ALREADY CORRECT (TAB_LABELS)
2. Pink animated underline tracks scroll position -- ALREADY CORRECT (underlineX + handlePageScroll)
3. PagerView enables swipe between all 4 tabs -- ALREADY CORRECT
4. Active tab shows Poppins_600SemiBold in colors.text, inactive shows Poppins_500Medium in colors.textSecondary -- ALREADY CORRECT
5. Tab bar sits between header and content, which means it's effectively "sticky" since the header is static (not scrollable) -- ALREADY CORRECT

The current architecture has: static header (PassportHeader) then PassportPager (tab bar + PagerView). Since the header doesn't scroll, the tab bar is always visible at its position. This IS the sticky behavior -- the tab bar never scrolls off screen.

If everything is correct, make NO changes to PassportPager. Only fix if something doesn't match the spec.

If minor polish is needed:
- Ensure tab bar has a bottom border or separator line (1px, colors.cardBorder) to visually separate from grid content
- If border already exists, leave it

Report: confirm the tab bar is effectively sticky, swipe works, underline animates.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel-mobile && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>Tab bar shows 4 tabs with pink underline. Swipe gestures work between all tabs. Tab bar is effectively sticky (header is static, tab bar never scrolls off). Separator line present.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Grid cells render as perfect squares (1:1 aspect ratio)
- Gaps between cells are 1px
- Overlay text shows 3 lines per cell (name, context, date)
- Founder badge (gold star) in top-right of founded cells
- Tab bar has 4 tabs, pink underline, swipe gestures
- Tab bar stays visible during scroll
</verification>

<success_criteria>
1. Grid cells are 1:1 squares with 1px gaps -- dense Instagram-style grid
2. Finds cells: artist name + platform dot/name + date
3. Founders cells: gold star badge top-right, artist name + platform + date
4. Discoveries cells: artist name + "via @username" + date
5. Tab bar pins during scroll, swipe between 4 tabs smooth, pink underline tracks position
6. No ghost/broken cells for empty positions (empty state shows)
</success_criteria>

<output>
After completion, create `.planning/phases/15-passport-redesign/15-02-SUMMARY.md`
</output>
