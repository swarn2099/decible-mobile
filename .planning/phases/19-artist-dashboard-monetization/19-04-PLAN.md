---
phase: 19-artist-dashboard-monetization
plan: 04
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - /home/swarn/decibel/src/app/api/dashboard/send-message/route.ts
  - /home/swarn/decibel/src/app/api/dashboard/messages/route.ts
  - /home/swarn/decibel/src/app/api/mobile/activity-feed/route.ts
  - /home/swarn/decibel-mobile/src/components/feed/ArtistMessageCard.tsx
  - /home/swarn/decibel-mobile/src/app/(tabs)/index.tsx
autonomous: true
requirements: [DASH-04, DASH-05, DASH-06]

must_haves:
  truths:
    - "Artist can compose and send a push notification from the dashboard"
    - "Rate limit of 1 message per week per artist is enforced"
    - "Push notifications arrive on fan devices within 60 seconds"
    - "Artist messages appear in the fan Home feed"
  artifacts:
    - path: "src/app/api/dashboard/send-message/route.ts"
      provides: "Push notification compose and send endpoint"
      exports: ["POST"]
    - path: "src/app/api/dashboard/messages/route.ts"
      provides: "Message history endpoint"
      exports: ["GET"]
    - path: "src/app/api/mobile/activity-feed/route.ts"
      provides: "Updated feed with artist message cards"
    - path: "/home/swarn/decibel-mobile/src/components/feed/ArtistMessageCard.tsx"
      provides: "Mobile feed card for artist messages"
  key_links:
    - from: "src/app/api/dashboard/send-message/route.ts"
      to: "src/lib/pushNotifications.ts"
      via: "sendBulkPushNotifications call"
      pattern: "sendBulkPushNotifications"
    - from: "src/app/api/mobile/activity-feed/route.ts"
      to: "artist_messages table"
      via: "Query for messages from collected artists"
      pattern: "artist_messages"
---

<objective>
Enable artists to send push notifications to their collectors from the dashboard, with messages appearing in the fan Home feed.

Purpose: Direct artist-to-fan communication is a key premium feature and drives engagement.
Output: Send message API, message history, mobile feed card, push notification delivery.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-artist-dashboard-monetization/19-CONTEXT.md

Working in: BOTH repos
- /home/swarn/decibel (web dashboard + API)
- /home/swarn/decibel-mobile (React Native fan app)

<interfaces>
From src/lib/pushNotifications.ts (already exists):
```typescript
export async function sendBulkPushNotifications(params: {
  userIds: string[];
  title: string;
  body: string;
  data: Record<string, string>;
  preferenceKey: PreferenceKey;
}): Promise<{ sent: number; skipped: number }>;
```
PreferenceKey includes 'artist_messages'.

From src/lib/types/artist-dashboard.ts:
```typescript
export interface ArtistMessage {
  id: string;
  artist_id: string;
  sender_user_id: string;
  message: string;
  message_type: 'general' | 'show_announcement';
  sent_at: string;
  recipient_count: number;
}
```

Mobile push token table: push_tokens (user_id, expo_push_token)
Mobile notification preferences: notification_preferences (user_id, artist_messages: boolean)
Mobile already has: expo-notifications installed, useNotifications hook, push token registration on launch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create send message and message history APIs</name>
  <files>
    /home/swarn/decibel/src/app/api/dashboard/send-message/route.ts,
    /home/swarn/decibel/src/app/api/dashboard/messages/route.ts
  </files>
  <action>
1. POST /api/dashboard/send-message:
   - Auth required: get user, look up artist_claims to get performer_id
   - Body: { message: string } (max 280 chars, validated)
   - Rate limit check: query artist_messages WHERE artist_id = performer_id AND sent_at > NOW() - 7 days. If any exist, return 429 with { error: "You can only send one message per week", next_available_at: last_message.sent_at + 7 days }
   - Get all fan_ids who collected this artist: SELECT DISTINCT fan_id FROM collections WHERE performer_id = X
   - Map fan_ids to auth user_ids: fans table has auth_user_id (or query by fan_id). Check fans table schema — fans.id is the fan_id, but push_tokens uses user_id (auth.users.id). Need to map: fans.auth_user_id or fans.email -> auth.users.id. Look at how existing pushNotifications.ts resolves this. The discover route uses fan_id directly as userId for sendPushNotification — so push_tokens.user_id = fans.id (the fan table ID, not auth user ID). Use fan_ids directly.
   - Call sendBulkPushNotifications with:
     - userIds: fan_ids array
     - title: performer.name (the artist's name)
     - body: the message text
     - data: { type: 'artist_message', artist_id: performer_id, message_id: new_message_id }
     - preferenceKey: 'artist_messages'
   - Insert into artist_messages: { artist_id: performer_id, sender_user_id: user.id, message, message_type: 'general', recipient_count: result.sent }
   - Return { success: true, sent: result.sent, skipped: result.skipped }

2. GET /api/dashboard/messages:
   - Auth required same pattern
   - Query artist_messages WHERE artist_id = performer_id ORDER BY sent_at DESC LIMIT 50
   - Return array of ArtistMessage objects
   - Include a `can_send` boolean: true if no message sent in last 7 days
   - Include `next_available_at` timestamp if rate limited
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Send message endpoint creates artist_messages entry, sends push notifications via Expo API, and enforces 1/week rate limit. History endpoint returns past messages with send eligibility.</done>
</task>

<task type="auto">
  <name>Task 2: Add artist message cards to mobile activity feed</name>
  <files>
    /home/swarn/decibel/src/app/api/mobile/activity-feed/route.ts,
    /home/swarn/decibel-mobile/src/components/feed/ArtistMessageCard.tsx,
    /home/swarn/decibel-mobile/src/app/(tabs)/index.tsx
  </files>
  <action>
1. Update GET /api/mobile/activity-feed/route.ts in the decibel web project:
   - After fetching regular feed items, also query artist_messages:
     - Get all performer_ids the user has collected
     - Query artist_messages WHERE artist_id IN (collected_performer_ids) AND sent_at > 7 days ago
     - Join with performers table for artist name, photo_url, slug
     - Map to feed items with type: 'artist_message', including message text, artist name, artist photo, sent_at
   - Merge artist_message items into the regular feed sorted by date (most recent first)
   - Each artist_message feed item shape: { type: 'artist_message', artist_name, artist_photo, artist_slug, message, created_at }

2. Create ArtistMessageCard.tsx in decibel-mobile:
   - Props: { artistName: string, artistPhoto: string | null, artistSlug: string, message: string, createdAt: string }
   - Design: Card with artist avatar on left, artist name in bold with a pink megaphone/speaker icon, message text below, time ago on right
   - Use theme colors from useTheme() hook (same pattern as other feed cards)
   - Tapping the card navigates to artist profile (router.push(`/artist/${artistSlug}`))
   - Distinct visual treatment: subtle pink left border (4px) to distinguish from organic feed items

3. Update the Home screen (index.tsx) in decibel-mobile:
   - Import ArtistMessageCard
   - In the feed FlatList renderItem, add a case for type === 'artist_message' that renders ArtistMessageCard
   - No other changes needed — the feed already handles mixed card types
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5 && cd /home/swarn/decibel-mobile && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Artist messages appear in the mobile activity feed with distinct visual treatment. Push notifications arrive on fan devices via existing Expo Push infrastructure.</done>
</task>

</tasks>

<verification>
- Web build passes
- Mobile TypeScript compiles
- POST /api/dashboard/send-message returns 429 on second send within 7 days
- Activity feed includes artist_message items for collected artists
</verification>

<success_criteria>
Artist can compose a 280-char message, send it to all collectors as push notifications, see delivery stats. Fans see artist messages in their Home feed with distinct styling. Rate limit enforced at 1/week.
</success_criteria>

<output>
After completion, create `.planning/phases/19-artist-dashboard-monetization/19-04-SUMMARY.md`
</output>
