---
phase: 19-artist-dashboard-monetization
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - /home/swarn/decibel/src/app/dashboard/page.tsx
  - /home/swarn/decibel/src/app/dashboard/claim/page.tsx
  - /home/swarn/decibel/src/app/dashboard/claim/claim-client.tsx
  - /home/swarn/decibel/src/app/api/dashboard/search-artists/route.ts
  - /home/swarn/decibel/src/app/api/dashboard/claim-artist/route.ts
autonomous: true
requirements: [DASH-01, DASH-12]

must_haves:
  truths:
    - "Artist can sign up with email, search for their artist, and submit a claim"
    - "Claimed artist shows verified badge after admin sets verified=true"
    - "Dashboard redirects unclaimed users to the claim flow"
  artifacts:
    - path: "src/app/dashboard/claim/page.tsx"
      provides: "Claim flow page with search and URL paste"
    - path: "src/app/api/dashboard/search-artists/route.ts"
      provides: "Search endpoint for artist claiming"
      exports: ["GET"]
    - path: "src/app/api/dashboard/claim-artist/route.ts"
      provides: "Claim submission endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/dashboard/claim/claim-client.tsx"
      to: "/api/dashboard/search-artists"
      via: "fetch on search input"
      pattern: "fetch.*dashboard/search-artists"
    - from: "src/app/dashboard/claim/claim-client.tsx"
      to: "/api/dashboard/claim-artist"
      via: "form submission"
      pattern: "fetch.*dashboard/claim-artist"
    - from: "src/app/dashboard/page.tsx"
      to: "artist_claims table"
      via: "Supabase query for claim status"
      pattern: "artist_claims"
---

<objective>
Build the artist claiming flow: signup, search for artist, submit claim, and verified badge display.

Purpose: This is the artist's entry point to Decibel. Without claiming, nothing else in the dashboard works.
Output: Claim flow pages + API endpoints + verified badge on artist profiles.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-artist-dashboard-monetization/19-CONTEXT.md

Working in: /home/swarn/decibel (Next.js web project)

<interfaces>
From src/lib/types/artist-dashboard.ts (created in Plan 01):
- ArtistClaim { id, user_id, performer_id, verified, verification_method, claimed_at }
- ArtistSubscription { id, artist_id, ... }

From existing codebase:
- createSupabaseAdmin() from src/lib/supabase-admin.ts
- createSupabaseServer() from src/lib/supabase-server.ts
- Auth callback at src/app/auth/callback/route.ts already handles claim redirect
- Middleware protects /dashboard routes
- Existing claim API at src/app/api/claim/route.ts (old, uses performers.claimed_by)
- Login page at src/app/auth/login/page.tsx (magic link OTP)
</interfaces>

Key decisions from CONTEXT.md:
- MVP verification = manual (Swarn sets verified=true in Supabase directly)
- Artist signs up with email (magic link, same Supabase auth)
- Artist searches by name OR pastes Spotify/SoundCloud/Apple Music URL
- System matches to existing performer in DB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create claim flow pages and search API</name>
  <files>
    /home/swarn/decibel/src/app/dashboard/claim/page.tsx,
    /home/swarn/decibel/src/app/dashboard/claim/claim-client.tsx,
    /home/swarn/decibel/src/app/api/dashboard/search-artists/route.ts
  </files>
  <action>
1. Create GET /api/dashboard/search-artists that:
   - Accepts ?q= query param (min 2 chars)
   - Searches performers table by name (ilike) OR by spotify_url/soundcloud_url/apple_music_url containing the query
   - Returns top 20 matches: { id, name, slug, photo_url, genres, city, claimed (boolean from existing column) }
   - No auth required (artists may search before claiming)

2. Create src/app/dashboard/claim/page.tsx (server component):
   - Check if user is logged in via createSupabaseServer()
   - If not logged in, redirect to /auth/login?redirect=/dashboard/claim
   - If logged in, check if they already have a claim in artist_claims table
   - If already claimed, redirect to /dashboard
   - Otherwise render ClaimClient

3. Create src/app/dashboard/claim/claim-client.tsx ("use client"):
   - Two-step flow:
     Step 1: Search for your artist
       - Text input with debounced search (300ms)
       - Results list showing artist name, photo, city, genres
       - Already-claimed artists shown grayed out with "Already claimed" badge
       - Each unclaimed result has a "This is me" button
     Step 2: Confirm and submit
       - Shows selected artist card
       - "Confirm Claim" button that POSTs to /api/dashboard/claim-artist
       - Success state: "Claim submitted! We'll verify your profile within 24 hours."
   - Use Tailwind classes matching existing dashboard theme (bg-bg, bg-bg-card, text-pink, border-light-gray/20)
   - Mobile responsive (full-width on small screens, max-w-lg centered on desktop)
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Claim page renders at /dashboard/claim with working search and artist selection UI.</done>
</task>

<task type="auto">
  <name>Task 2: Create claim API + update dashboard page + verified badge</name>
  <files>
    /home/swarn/decibel/src/app/api/dashboard/claim-artist/route.ts,
    /home/swarn/decibel/src/app/dashboard/page.tsx
  </files>
  <action>
1. Create POST /api/dashboard/claim-artist:
   - Auth required: get user from createSupabaseServer().auth.getUser()
   - Body: { performer_id: string }
   - Validate: performer exists, performer not already claimed (check artist_claims table), user doesn't already have a claim
   - Insert into artist_claims: { user_id: user.id, performer_id, verified: false, verification_method: 'manual' }
   - Also update performers table: SET claimed = true, claimed_by = user.id (maintain backward compat with existing claim system)
   - Also create trial subscription in artist_subscriptions: { artist_id: performer_id, plan: 'trial', trial_ends_at: NOW() + 14 days, status: 'trialing' }
   - Return { success: true, claim_id }

2. Update src/app/dashboard/page.tsx:
   - Replace existing ClaimPrompt with redirect to /dashboard/claim
   - Query artist_claims table instead of just performers.claimed_by:
     ```
     const { data: claim } = await admin
       .from('artist_claims')
       .select('*, performers(*)')
       .eq('user_id', user.id)
       .single();
     ```
   - If no claim, redirect to /dashboard/claim
   - If claim exists but not verified, show "Pending Verification" banner with message
   - If claim exists and verified, show full dashboard (reuse existing DashboardClient for now, will be rebuilt in Plan 03)
   - Pass performer.verified to DashboardClient

3. Update the performers query on artist/[slug]/page.tsx to show a verified checkmark badge next to the artist name if performers.verified is true. Add a small shield/checkmark icon (lucide `BadgeCheck`) in pink next to the name. This is the verified badge (DASH-12).
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Artist can search, select, and claim a profile. Dashboard shows pending/verified state. Verified badge appears on public artist pages.</done>
</task>

</tasks>

<verification>
- Build passes with no errors
- /dashboard/claim page renders search UI
- POST /api/dashboard/claim-artist creates entries in artist_claims and artist_subscriptions
- /dashboard redirects to /dashboard/claim for unclaimed users
- Verified badge visible on artist/[slug] page when verified=true
</verification>

<success_criteria>
End-to-end claiming flow works: artist signs up -> searches -> claims -> sees pending state -> admin verifies -> dashboard accessible. Verified badge shows on public profile.
</success_criteria>

<output>
After completion, create `.planning/phases/19-artist-dashboard-monetization/19-02-SUMMARY.md`
</output>
