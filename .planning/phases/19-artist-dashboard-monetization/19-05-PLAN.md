---
phase: 19-artist-dashboard-monetization
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - /home/swarn/decibel/src/app/api/dashboard/shows/route.ts
  - /home/swarn/decibel/src/app/api/dashboard/shows/notify/route.ts
  - /home/swarn/decibel/src/app/dashboard/dashboard-client.tsx
autonomous: true
requirements: [DASH-07]

must_haves:
  truths:
    - "Artist can create a show listing from the dashboard"
    - "Artist can push notify collectors within a radius about the show"
    - "Show notification includes venue, date, and ticket link"
  artifacts:
    - path: "src/app/api/dashboard/shows/route.ts"
      provides: "CRUD for artist shows"
      exports: ["GET", "POST"]
    - path: "src/app/api/dashboard/shows/notify/route.ts"
      provides: "Radius-based push notification for shows"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/dashboard/shows/notify/route.ts"
      to: "src/lib/pushNotifications.ts"
      via: "sendBulkPushNotifications with filtered fan list"
      pattern: "sendBulkPushNotifications"
    - from: "dashboard-client.tsx Shows tab"
      to: "/api/dashboard/shows"
      via: "fetch for CRUD"
      pattern: "fetch.*dashboard/shows"
---

<objective>
Build the Smart Flyer feature: artists create show listings and push notify collectors within a specified radius.

Purpose: Show promotion is high-value for artists and drives real-world engagement.
Output: Shows CRUD API + radius notification + Shows tab in dashboard.
</objective>

<execution_context>
@/home/swarn/.claude/get-shit-done/workflows/execute-plan.md
@/home/swarn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-artist-dashboard-monetization/19-CONTEXT.md

Working in: /home/swarn/decibel (Next.js web project)

<interfaces>
From src/lib/types/artist-dashboard.ts:
```typescript
export interface ArtistShow {
  id: string;
  artist_id: string;
  venue_name: string;
  venue_city: string | null;
  venue_lat: number | null;
  venue_lng: number | null;
  show_date: string;
  ticket_url: string | null;
  description: string | null;
  notification_sent: boolean;
  notification_radius_miles: number;
  created_at: string;
}
```

From src/lib/pushNotifications.ts:
- sendBulkPushNotifications({ userIds, title, body, data, preferenceKey })

Fan location: fans table may have city column. For radius calculation, we need lat/lng.
Approach: Since fans don't have lat/lng stored, use city-level matching for MVP.
Alternative: Use a simple geocoding approach — match venue_city against fans.city for MVP radius.
Actually, for true radius: the fan app stores location during check-ins. But for push targeting, use fans.city as a proxy for MVP. The notification_radius_miles column exists for future precision.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shows CRUD and notification APIs</name>
  <files>
    /home/swarn/decibel/src/app/api/dashboard/shows/route.ts,
    /home/swarn/decibel/src/app/api/dashboard/shows/notify/route.ts
  </files>
  <action>
1. GET /api/dashboard/shows:
   - Auth required: user -> artist_claims -> performer_id
   - Query artist_shows WHERE artist_id = performer_id ORDER BY show_date DESC
   - Return array of ArtistShow

2. POST /api/dashboard/shows:
   - Auth required same
   - Body: { venue_name, venue_city?, show_date, ticket_url?, description?, notification_radius_miles? (default 50) }
   - Validate: venue_name required, show_date required and in the future
   - Insert into artist_shows, return the created show

3. POST /api/dashboard/shows/notify:
   - Auth required same
   - Body: { show_id: string }
   - Look up the show, verify it belongs to this artist
   - Check notification_sent — if true, return 409 "Already notified"
   - Get all fan_ids who collected this artist
   - For MVP city-based radius: if venue_city is set, filter fans WHERE fans.city ILIKE venue_city. If no venue_city or no city matches, send to ALL collectors (better to over-send than miss).
   - Build notification:
     - title: "[Artist Name] is playing live!"
     - body: "[Venue Name] on [formatted date] — tap for details"
     - data: { type: 'show_announcement', show_id, ticket_url (if exists) }
     - preferenceKey: 'artist_messages' (reuse same pref key)
   - Call sendBulkPushNotifications
   - Update artist_shows SET notification_sent = true, recipient_count (add this update)
   - Also insert into artist_messages as message_type = 'show_announcement' so it appears in the feed
   - Return { success: true, sent, skipped }
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Shows CRUD works. Notification endpoint sends push to collectors and marks show as notified.</done>
</task>

<task type="auto">
  <name>Task 2: Add Shows tab to dashboard client</name>
  <files>/home/swarn/decibel/src/app/dashboard/dashboard-client.tsx</files>
  <action>
Update the dashboard-client.tsx to implement the "Shows" tab (currently placeholder from Plan 03).

Shows tab UI:
1. "Create Show" form at top:
   - Venue name (text input, required)
   - City (text input, optional)
   - Date and time (datetime-local input, required)
   - Ticket URL (text input, optional)
   - Description (textarea, optional)
   - Notification radius (select: 25/50/100/250 miles, default 50)
   - "Create Show" button (gradient pink->purple)

2. Shows list below:
   - Cards for each show: venue name bold, city, formatted date, ticket link
   - Status badge: "Upcoming" (teal) or "Past" (gray) based on show_date vs now
   - "Notify Fans" button (pink) — only for upcoming shows where notification_sent = false
   - "Notified" badge (green checkmark) for shows already notified
   - Notification count shown: "Sent to 47 fans"

3. Fetch shows list on tab mount. Create show via POST, refresh list. Notify via POST /shows/notify.

Use same styling patterns: bg-bg-card, border-light-gray/10, rounded-xl, text-sm, etc.
  </action>
  <verify>
    <automated>cd /home/swarn/decibel && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Shows tab renders create form, shows list, and working notify button. Full Smart Flyer feature functional.</done>
</task>

</tasks>

<verification>
- Build passes
- Shows tab creates and lists shows
- Notify endpoint sends push notifications and updates show status
</verification>

<success_criteria>
Artist can create a show listing with venue/date/ticket details, then push notify collectors with one tap. Show appears in fans' notification feed.
</success_criteria>

<output>
After completion, create `.planning/phases/19-artist-dashboard-monetization/19-05-SUMMARY.md`
</output>
